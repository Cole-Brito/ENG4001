import 'package:flutter/material.dart';

/// Central colour palette for the whole app.
/// Add more semantic colours here when needed.
class AppColors {
  static const primary = Color(0xFF10138A);
  static const primaryLight = Color(0xFFE3F2FD);
  static const accent = Colors.indigoAccent;
  static const error = Colors.redAccent;
  static const background = Colors.white;
  static const text = Colors.black87;
}
